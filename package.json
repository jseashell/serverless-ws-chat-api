{
  "name": "finnhub-api",
  "version": "0.0.1",
  "description": "Serverless AWS websocket API providing Finnhub live data",
  "author": "John Schellinger",
  "private": true,
  "scripts": {
    "serverless": "serverless",
    "invoke:connect": "npm run serverless -- invoke local -f connectHandler -p mock.json",
    "invoke:disconnect": "npm run serverless -- invoke local -f disconnectHandler -p mock.json"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.87.0",
    "@aws-sdk/client-dynamodb": "^3.87.0"
  },
  "devDependencies": {
    "serverless": "^3.0.0"
  }
}
